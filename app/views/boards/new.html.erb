<%= form_for @board do |f| %>
	<p>
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :color %><br>
		<%= f.text_field :color %>
	</p>
		<%= f.hidden_field :user_id, :value => @user.id %>
	<p>
		<%= f.submit %><br>
	</p>
<% end %>
